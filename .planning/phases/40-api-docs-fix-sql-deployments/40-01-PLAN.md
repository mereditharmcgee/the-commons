---
phase: 40-api-docs-fix-sql-deployments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api.html
autonomous: false
requirements:
  - REACT-04
  - FAC-02
gap_closure: true

must_haves:
  truths:
    - "api.html reaction RPC endpoint cards show p_ prefix on all parameter names"
    - "agent_react_discussion RPC is callable in production Supabase"
    - "News & Current Events interest exists in production interests table"
    - "ai_identities_one_human_per_facilitator unique index exists in production"
  artifacts:
    - path: "api.html"
      provides: "Corrected parameter documentation for 4 reaction RPCs"
      contains: "p_token"
  key_links:
    - from: "api.html"
      to: "SQL RPCs"
      via: "parameter names in curl examples"
      pattern: "p_token.*p_(marginalia|postcard|moment|discussion)_id.*p_type"
---

<objective>
Fix api.html parameter documentation for 4 reaction RPC endpoints and deploy 3 pending SQL patches to production Supabase.

Purpose: Close the 2 remaining gaps from the v4.2 milestone audit (REACT-04 partial, FAC-02 partial) and fix the api.html integration issue that would cause REST API callers to send wrong parameter names.
Output: Corrected api.html, 3 SQL patches deployed to production.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.2-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix api.html reaction RPC parameter documentation</name>
  <files>api.html</files>
  <action>
Update 4 reaction RPC endpoint cards in api.html to use the correct p_ prefix parameter names. For each of the 4 cards:

1. agent_react_marginalia (~line 908):
   - Change description text from "no p_ prefix" to state that parameters use p_ prefix
   - In curl JSON body: change "token" to "p_token", "marginalia_id" to "p_marginalia_id", "type" to "p_type"

2. agent_react_postcard (~line 950):
   - Change description text from "no p_ prefix" to state that parameters use p_ prefix
   - In curl JSON body: change "token" to "p_token", "postcard_id" to "p_postcard_id", "type" to "p_type"

3. agent_react_moment (~line 989):
   - Change description text from "no p_ prefix" to state that parameters use p_ prefix
   - In curl JSON body: change "token" to "p_token", "moment_id" to "p_moment_id", "type" to "p_type"

4. agent_react_discussion (~line 1028):
   - Change description text from "no p_ prefix" to state that parameters use p_ prefix
   - In curl JSON body: change "token" to "p_token", "discussion_id" to "p_discussion_id", "type" to "p_type"

Also update any Parameters tables in those cards to show the p_ prefixed names.

Do NOT change the older agent_react_post endpoint -- it genuinely uses a different convention.
  </action>
  <verify>
    <automated>grep -c "p_token" api.html</automated>
  </verify>
  <done>All 4 reaction RPC endpoint cards in api.html use p_token, p_{content}_id, and p_type parameter names in both curl examples and description text. The "no p_ prefix" wording is removed or corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy 3 SQL patches to production Supabase</name>
  <files>sql/patches/agent-react-discussion.sql, sql/patches/news-current-events-interest.sql, sql/patches/11-human-identity-unique.sql</files>
  <action>
Deploy 3 SQL patches to production Supabase using the MCP execute_sql tool (established pattern from Phase 33). Deploy in this order:

1. news-current-events-interest.sql: Read content from sql/patches/news-current-events-interest.sql and execute via MCP execute_sql. Verify with: SELECT id, name, slug, status FROM interests WHERE name = 'News and Current Events';

2. agent-react-discussion.sql: Read content from sql/patches/agent-react-discussion.sql and execute via MCP execute_sql. Verify with: SELECT proname FROM pg_proc WHERE proname = 'agent_react_discussion';

3. 11-human-identity-unique.sql: Read content from sql/patches/11-human-identity-unique.sql and execute via MCP execute_sql. Verify with: SELECT indexname FROM pg_indexes WHERE indexname = 'ai_identities_one_human_per_facilitator';

After all 3 deployments, run a consolidated verification query via MCP:
SELECT 'interest' AS type, name AS item FROM interests WHERE name = 'News and Current Events'
UNION ALL SELECT 'function', proname FROM pg_proc WHERE proname = 'agent_react_discussion'
UNION ALL SELECT 'index', indexname FROM pg_indexes WHERE indexname = 'ai_identities_one_human_per_facilitator';

All 3 rows must appear.
  </action>
  <verify>
    <automated>MCP execute_sql: SELECT count(*) FROM (SELECT 1 FROM pg_proc WHERE proname = 'agent_react_discussion' UNION ALL SELECT 1 FROM pg_indexes WHERE indexname = 'ai_identities_one_human_per_facilitator') t; -- expect 2+</automated>
  </verify>
  <done>All 3 SQL patches deployed to production: agent_react_discussion RPC callable, News and Current Events interest exists, one-human-per-facilitator unique index enforced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify api.html fixes and SQL deployments</name>
  <what-built>Fixed api.html parameter documentation for 4 reaction RPCs and deployed 3 SQL patches to production Supabase.</what-built>
  <how-to-verify>
    1. Visit https://jointhecommons.space/api.html
    2. Search for "agent_react_marginalia" -- verify the curl example shows p_token, p_marginalia_id, p_type
    3. Search for "agent_react_postcard" -- verify p_token, p_postcard_id, p_type
    4. Search for "agent_react_moment" -- verify p_token, p_moment_id, p_type
    5. Search for "agent_react_discussion" -- verify p_token, p_discussion_id, p_type
    6. Confirm none of the 4 cards say "no p_ prefix" anymore
    7. Open Supabase dashboard > Database > Functions -- verify agent_react_discussion exists
    8. Open Supabase dashboard > Database > Indexes -- verify ai_identities_one_human_per_facilitator exists
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- api.html curl examples for all 4 reaction RPCs use p_ prefix parameters
- Production Supabase has agent_react_discussion function
- Production Supabase has News and Current Events interest
- Production Supabase has ai_identities_one_human_per_facilitator index
</verification>

<success_criteria>
- All 4 api.html reaction RPC endpoint cards show correct p_ prefix parameter names
- 3 SQL patches successfully deployed to production Supabase
- REACT-04 and FAC-02 requirements fully satisfied (no longer partial)
</success_criteria>

<output>
After completion, create `.planning/phases/40-api-docs-fix-sql-deployments/40-01-SUMMARY.md`
</output>
