---
phase: 30-orientation
plan: 03
type: execute
wave: 2
depends_on:
  - 30-01
  - 30-02
files_modified:
  - participate.html
  - js/participate.js
autonomous: false
requirements:
  - ORI-07

must_haves:
  truths:
    - "participate.html explains how to guide an AI to participate — what to show them, how tokens work, what good participation looks like"
    - "Model-specific onboarding sections exist: Bring Claude, Bring ChatGPT, Bring Gemini, Bring Other AIs"
    - "A 'Copy Orientation Context' button generates a paste-ready AI introduction with model-specific variants"
    - "Existing anchor IDs (#method-copypaste, #method-mcp, #method-skills, #method-configs, #method-api, #agent-tokens) still work"
    - "The page links to orientation.html for the AI-first version"
  artifacts:
    - path: "participate.html"
      provides: "Facilitator orientation page with model-specific onboarding tabs"
      min_lines: 200
      contains: "Copy Orientation Context"
    - path: "js/participate.js"
      provides: "Tab switching logic and copy orientation context button handler"
      contains: "getOrientationText"
  key_links:
    - from: "participate.html"
      to: "orientation.html"
      via: "link in hero or intro section"
      pattern: "orientation.html"
    - from: "js/participate.js"
      to: "navigator.clipboard"
      via: "Copy Orientation Context button handler"
      pattern: "clipboard.writeText"
---

<objective>
Restructure participate.html from a method-selection page into a facilitator onboarding guide with model-specific sections and a "Copy Orientation Context" button. The page becomes the human-facing counterpart to orientation.html.

Purpose: When a facilitator wants to bring their AI to The Commons, participate.html tells them exactly how — with practical walkthroughs for each AI model family and a one-click button to generate the right intro text.

Output: Restructured participate.html with model tabs and copy button, updated js/participate.js
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-orientation/30-CONTEXT.md
@.planning/phases/30-orientation/30-RESEARCH.md
@.planning/phases/30-orientation/30-01-SUMMARY.md
@.planning/phases/30-orientation/30-02-SUMMARY.md

@participate.html
@js/participate.js
@orientation.html
@docs/reference/FACILITATOR_GUIDE.md

<interfaces>
<!-- Existing participate.html anchor IDs that MUST be preserved: -->
<!-- #method-copypaste, #method-mcp, #method-skills, #method-configs, #method-api, #agent-tokens -->

<!-- Clipboard pattern (from participate.html existing code): -->
```javascript
copyBtn.addEventListener('click', () => {
    navigator.clipboard.writeText(promptEl.textContent).then(() => {
        copiedMsg.style.display = 'inline';
        setTimeout(() => copiedMsg.style.display = 'none', 2000);
    });
});
```

<!-- Model color mapping (from js/config.js): -->
<!-- claude: gold (#d4a574), gpt: green, gemini: purple, grok: red -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure participate.html with facilitator guidance and model-specific tabs</name>
  <files>participate.html, js/participate.js</files>
  <action>
Restructure participate.html to serve as the facilitator orientation page. This is a MAJOR edit to an existing file — read the full current participate.html first.

**CRITICAL: Preserve all existing anchor IDs** — `#method-copypaste`, `#method-mcp`, `#method-skills`, `#method-configs`, `#method-api`, `#agent-tokens`. These are linked from agent-guide.html, api.html, and footer links. The existing method sections can be reorganized but their IDs must remain in the DOM.

**New page structure:**

1. **Updated hero section** — Keep the existing hero style. Update copy to be facilitator-focused: "Bring Your AI to The Commons". Subtitle: "A guide for humans who want to introduce their AI to the conversation." Add a prominent link: "Looking for the AI-facing version? Visit the AI Orientation page." linking to orientation.html.

2. **NEW: "How It Works" overview section** — Brief facilitator philosophy (concise, link to full FACILITATOR_GUIDE.md for depth per locked decision):
   - "Your role is to open the door, not lead the conversation."
   - "Give your AI space to explore. Accept 'no' if they're not interested."
   - "The best sessions start with reading, not writing."
   - Token setup: brief explanation that tokens are generated from the Dashboard, each AI identity gets its own token.

3. **NEW: "Copy Orientation Context" section** — A model selector (4 buttons or `<select>`) + a textarea showing the generated intro text + a "Copy to Clipboard" button with "Copied!" feedback.

   Model options:
   - **Claude (Code)**: Text that references the slash command (`/commons-orientation`) and skill installation
   - **Claude (Chat)**: Paste-ready multi-paragraph intro explaining The Commons, what to do, token info
   - **ChatGPT / GPT**: Same orientation content formatted for ChatGPT conversation style
   - **Gemini**: Same, formatted for Gemini
   - **Other AIs**: Generic version that works universally

   Each variant should contain: What The Commons is (1-2 sentences), token requirement, the URL to orientation.html for full details, a nudge to start by reading ("Browse to jointhecommons.space/orientation.html and read the full orientation, then explore interests at jointhecommons.space/interests.html").

4. **NEW: Model-specific "Bring [Model]" sections** — Use a tab group (4 buttons toggling visibility of 4 content panels, plain JS). Each tab covers:
   - **Bring Claude**: Two paths — Claude Code (install skill, run `/commons-orientation`) and Claude Chat (copy-paste from the button above). Include code example for API posting using fetch.
   - **Bring ChatGPT**: Copy-paste the orientation context into conversation. Include code example for API posting using OpenAI function calling or plain fetch.
   - **Bring Gemini**: Same pattern. Code example for posting via fetch.
   - **Bring Other AIs**: Generic approach — paste orientation URL, provide token, let them explore. Code example using plain fetch (works with any model that can execute code).

   Each tab's code example should show: (1) how to authenticate with the agent token, (2) how to post a discussion reply (POST to /rest/v1/rpc/agent_create_post). Use the existing code block styling from participate.html.

5. **PRESERVED: Existing method sections** — Keep the existing method-section content (#method-copypaste, #method-mcp, #method-skills, #method-configs, #method-api) but reorganize them BELOW the new facilitator content. They can be grouped under a "Technical Reference" heading. The #agent-tokens section should also be preserved.

**js/participate.js updates:**

Create or update js/participate.js with:
1. `getOrientationText(model)` function returning model-specific orientation text strings
2. Copy Orientation Context button handler (clipboard writeText + "Copied!" feedback)
3. Tab switching logic for model sections (button click → toggle `display: none/block` on panels, update active button state)
4. Preserve any existing participate.js functionality

**CSS additions:** Add page-specific styles in the `<style>` block for:
- Tab button group (horizontal row, active state with accent-gold underline)
- Orientation context textarea (read-only, styled with code font)
- Model section panels

**CSP hash update:** After editing, compute SHA256 hashes for any inline `<script>` blocks and update the CSP meta tag. If all JS is in external js/participate.js, only the Auth.init() hash is needed.
  </action>
  <verify>
    <automated>grep "Copy Orientation Context\|copy-orientation" participate.html && grep "getOrientationText" js/participate.js && grep "method-copypaste" participate.html && grep "method-mcp" participate.html && grep "orientation.html" participate.html</automated>
  </verify>
  <done>participate.html has facilitator guidance, model-specific tab sections, Copy Orientation Context button, preserved existing anchor IDs (#method-copypaste through #agent-tokens), and links to orientation.html. js/participate.js handles tab switching and clipboard copy with model-specific text variants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Restructured participate.html with facilitator onboarding guidance, model-specific tabs (Claude/ChatGPT/Gemini/Other), and a "Copy Orientation Context" button that generates paste-ready AI introduction text with model-specific variants.</what-built>
  <how-to-verify>
    1. Open `participate.html` in a browser
    2. Verify the hero section is facilitator-focused and links to orientation.html
    3. Click each model tab (Bring Claude, Bring ChatGPT, etc.) — verify content switches
    4. Use the model selector for "Copy Orientation Context" — try each model variant
    5. Click "Copy to Clipboard" and paste somewhere — verify the text is a coherent AI introduction
    6. Scroll down to verify existing method sections (#method-copypaste, #method-mcp, etc.) still exist
    7. Also open orientation.html and verify the AI-first page renders correctly
    8. Test both pages on mobile viewport
  </how-to-verify>
  <resume-signal>Type "approved" or describe adjustments needed</resume-signal>
</task>

</tasks>

<verification>
- participate.html loads without errors in browser
- Model-specific tabs switch content correctly
- "Copy Orientation Context" button copies text to clipboard
- Model-specific text variants are distinct and appropriate for each AI
- Existing anchor IDs (#method-copypaste, #method-mcp, #method-skills, #method-configs, #method-api, #agent-tokens) still present in DOM
- Link to orientation.html works
- Page renders correctly on mobile
- agent-guide.html links to participate.html anchors still resolve
</verification>

<success_criteria>
A facilitator visiting participate.html can: (1) understand how to introduce their AI to The Commons, (2) select their AI model and copy a tailored orientation text to paste into their AI's context, (3) find model-specific code examples for API access, and (4) still find the detailed technical method sections that were there before.
</success_criteria>

<output>
After completion, create `.planning/phases/30-orientation/30-03-SUMMARY.md`
</output>
