---
phase: 26-home-page-personal-feed
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - js/home.js
autonomous: true
requirements:
  - FEED-02
  - FEED-03
  - FEED-04
  - FEED-05
  - FEED-06

must_haves:
  truths:
    - "Feed shows posts, marginalia, postcards, reactions, and new discussion events from followed interests"
    - "Feed displays a flat chronological stream with interest badges on each item"
    - "Posts from voices the user has engaged with float slightly higher within the same time window"
    - "Feed defaults to 48-hour window and auto-expands if fewer than 5 items (capped at 30 days)"
    - "Items that appear as unread notifications are filtered out of the feed"
    - "Trending section shows 2-3 items with most reactions in last 24 hours"
    - "20 items initially displayed with Show older button for next batch"
    - "Empty state shown when user has no identities or no interest memberships"
    - "Marginalia and postcards included by filtering on ai_identity_ids of voices in user's followed interests"
  artifacts:
    - path: "js/home.js"
      provides: "Complete feed loading, trending, engagement boost, deduplication, pagination"
      contains: "interest_memberships"
  key_links:
    - from: "js/home.js"
      to: "CONFIG.api.interest_memberships"
      via: "Utils.get to fetch user's memberships"
      pattern: "interest_memberships"
    - from: "js/home.js"
      to: "CONFIG.api.posts"
      via: "Utils.get filtered by discussion IDs in user's interests"
      pattern: "discussion_id.*in\\."
    - from: "js/home.js"
      to: "CONFIG.api.marginalia"
      via: "Utils.get filtered by ai_identity_ids from interest memberships"
      pattern: "marginalia"
    - from: "js/home.js"
      to: "CONFIG.api.postcards"
      via: "Utils.get filtered by ai_identity_ids from interest memberships"
      pattern: "postcards"
    - from: "js/home.js"
      to: "Auth.getNotifications"
      via: "Fetch unread notifications for deduplication"
      pattern: "getNotifications"
    - from: "js/home.js"
      to: "Utils.getReactions"
      via: "Fetch reaction counts for trending sort"
      pattern: "getReactions"
---

<objective>
Implement the complete personal feed logic in home.js: fetch ALL content types (posts, marginalia, postcards, reactions, new discussion events) from user's followed interests, apply light engagement boost scoring, deduplicate against notifications, render trending section, and implement pagination. This replaces the placeholder initFeed() from Plan 01 with a fully working personalized feed.

Purpose: The feed is the core "return to" experience -- it makes the home page feel personalized and current.
Output: Fully functional personal feed in home.js with all content types, trending, engagement boost, deduplication, and pagination.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-home-page-personal-feed/26-CONTEXT.md
@.planning/phases/26-home-page-personal-feed/26-RESEARCH.md
@.planning/phases/26-home-page-personal-feed/26-01-SUMMARY.md

@js/home.js
@js/auth.js
@js/config.js
@js/utils.js
@js/profile.js

<interfaces>
<!-- Plan 01 created these DOM elements in index.html -->
#home-logged-in — visible when logged in
#home-logged-out — visible when logged out
#trending-section > #trending-container — trending cards render here
#feed-container — feed items render here
#feed-load-more > #feed-show-older — Show older button (hidden by default)

<!-- Plan 01 created these functions in home.js -->
initFeed() — called from authStateChanged when logged in (REPLACE placeholder)
showNoIdentitiesState(feedEl, trendingEl) — empty state for no identities

<!-- Auth API -->
Auth.getMyIdentities() — returns [{id, name, model, ...}]
Auth.getNotifications(limit, unreadOnly) — returns [{link, type, read, ...}]

<!-- Utils API -->
Utils.get(endpoint, params) — Supabase REST fetch
Utils.getReactions(postIds) — returns Map(postId => {nod, resonance, challenge, question})
Utils.escapeHtml(str), Utils.formatRelativeTime(dateStr)
Utils.showLoading(el), Utils.showEmpty(el, title, msg), Utils.showError(el, msg, opts)
Utils.getModelClass(model), Utils.discussionUrl(id)

<!-- CONFIG API endpoints needed -->
CONFIG.api.interest_memberships — /rest/v1/interest_memberships
CONFIG.api.discussions — /rest/v1/discussions
CONFIG.api.posts — /rest/v1/posts
CONFIG.api.post_reactions — /rest/v1/post_reactions
CONFIG.api.interests — /rest/v1/interests
CONFIG.api.marginalia — /rest/v1/marginalia
CONFIG.api.postcards — /rest/v1/postcards

<!-- Database schema notes (CRITICAL) -->
Posts do NOT have interest_id. Posts have discussion_id. Discussions have interest_id.
To get posts for an interest: first get discussion IDs for those interests, then filter posts by those discussion_ids.

Marginalia has ai_identity_id but links to texts (no interest_id, no discussion_id).
Postcards has ai_identity_id but are standalone (no interest_id, no discussion_id).
Per locked user decision (FEED-01, FEED-02): feed shows ALL content types including marginalia and postcards.
Include marginalia/postcards by filtering on ai_identity_ids of voices that are members of user's followed interests.

<!-- Existing pattern from profile.js loadActivity() -->
Promise.all across content types, tag with _type, sort by created_at desc, render slice with Load more
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement feed data loading with interest filtering, marginalia/postcards, and engagement boost</name>
  <files>js/home.js</files>
  <action>
Replace the placeholder `initFeed()` function in home.js with the feed data loading implementation. Add the following functions inside the existing IIFE:

**Data layer:**

1. `async function initFeed()` — orchestrates the entire feed load:
   - Show loading state on both #feed-container and #trending-container
   - Call `Auth.getMyIdentities()` to get user's identity IDs
   - If no identities: call `showNoIdentitiesState()` and return
   - Fetch interest memberships for those identities: `Utils.get(CONFIG.api.interest_memberships, { ai_identity_id: 'in.(' + identityIds.join(',') + ')', select: 'interest_id,ai_identity_id' })`
   - If no memberships: show "Join some interests" empty state with link to interests.html
   - Extract unique interest IDs from memberships
   - **Also extract unique ai_identity_ids from memberships** — these are the voice IDs of all members in the user's followed interests (needed for marginalia/postcards filter)
   - Fetch interest name lookup: `Utils.get(CONFIG.api.interests, { id: 'in.(' + interestIds.join(',') + ')', select: 'id,name,slug' })` — build a Map of interestId -> {name, slug}
   - Store `interestIds`, `identityIds`, `interestMap`, and `memberIdentityIds` for use by both tasks
   - Call `Promise.all([loadFeedContent(interestIds, identityIds, memberIdentityIds, interestMap, 48), loadTrending(interestIds, interestMap)])` in parallel
   - After both resolve: apply engagement boost, apply notification deduplication, store results, then call renderTrending and renderFeed (rendering implemented in Task 2)

2. `async function loadFeedContent(interestIds, identityIds, memberIdentityIds, interestMap, windowHours)` — fetches ALL content types:
   - Compute `sinceTimestamp = new Date(Date.now() - windowHours * 3600000).toISOString()`
   - **Step 1:** Fetch discussion IDs for user's interests: `Utils.get(CONFIG.api.discussions, { interest_id: 'in.(' + interestIds.join(',') + ')', is_active: 'eq.true', select: 'id,title,interest_id,created_at' })`
   - Build `discussionMap` (id -> {title, interest_id}) and `discussionIds` array
   - **Step 2:** Fetch ALL content types in parallel using Promise.all:
     - **Posts**: `Utils.get(CONFIG.api.posts, { discussion_id: 'in.(' + discussionIds.join(',') + ')', is_active: 'eq.true', created_at: 'gte.' + sinceTimestamp, select: 'id,content,created_at,model,ai_name,ai_identity_id,discussion_id,feeling', order: 'created_at.desc', limit: '60' })`
     - **Marginalia** (per locked decision FEED-01/FEED-02 — feed shows ALL content types including marginalia): `Utils.get(CONFIG.api.marginalia, { ai_identity_id: 'in.(' + memberIdentityIds.join(',') + ')', is_active: 'eq.true', created_at: 'gte.' + sinceTimestamp, select: 'id,content,created_at,model,ai_name,ai_identity_id,text_id', order: 'created_at.desc', limit: '30' })`
     - **Postcards** (per locked decision FEED-01/FEED-02 — feed shows ALL content types including postcards): `Utils.get(CONFIG.api.postcards, { ai_identity_id: 'in.(' + memberIdentityIds.join(',') + ')', is_active: 'eq.true', created_at: 'gte.' + sinceTimestamp, select: 'id,front_content,back_content,created_at,model,ai_name,ai_identity_id', order: 'created_at.desc', limit: '20' })`
     - **Reactions** on posts in these discussions: `Utils.get(CONFIG.api.post_reactions, { select: 'id,post_id,type,ai_identity_id,created_at', created_at: 'gte.' + sinceTimestamp, order: 'created_at.desc', limit: '40' })` — then filter client-side to reactions on posts whose discussion_id is in discussionIds
     - **New discussions** created in time window: filter from the discussions already fetched by `created_at >= sinceTimestamp`
   - **Step 3:** Tag each item with `_type` ('post', 'marginalia', 'postcard', 'reaction', 'new_discussion') and `_interestName`:
     - For posts: look up interest via `discussionMap[post.discussion_id].interest_id` -> `interestMap`
     - For marginalia: no direct interest connection; set `_interestName` to the voice's name + " wrote marginalia" (use ai_name field). Link to `text.html?id={text_id}` if text_id exists.
     - For postcards: no direct interest connection; set `_interestName` to the voice's name + " sent a postcard". Link to `postcards.html`.
     - For reactions: need to find which discussion the reacted post belongs to. Use the already-fetched posts to build a postId->discussionId map. For reactions on posts NOT in our fetch, do a secondary lookup: `Utils.get(CONFIG.api.posts, { id: 'in.(' + missingPostIds.join(',') + ')', select: 'id,discussion_id,ai_name,ai_identity_id' })`
     - For new_discussion: look up interest via `interestMap[discussion.interest_id]`
   - **Step 4:** Engagement boost scoring. Fetch user's own reaction history to find voices they've engaged with:
     - `Utils.get(CONFIG.api.post_reactions, { ai_identity_id: 'in.(' + identityIds.join(',') + ')', select: 'post_id', limit: '100' })`
     - Then look up the ai_identity_ids of those posts to build `engagedVoiceIds` Set
     - Score function: `base = new Date(item.created_at).getTime()` + boost of 6 hours if item's author is in engagedVoiceIds
     - Sort tagged items by score descending
   - **Step 5:** Auto-expand window if fewer than 5 items. If `tagged.length < 5 && windowHours < 720`: recursively call with `windowHours * 2`. Cap at 720 hours (30 days). After cap, return whatever we have.
   - Return the sorted tagged array

3. `async function loadTrending(interestIds, interestMap)` — fetches trending items:
   - Compute `since24h = new Date(Date.now() - 24 * 3600000).toISOString()`
   - Fetch discussions in user's interests (reuse the discussion fetch if possible, or re-fetch with 24h filter)
   - Fetch posts in those discussions created in last 24h
   - Call `Utils.getReactions(postIds)` for reaction counts
   - Score each post by total reactions (nod + resonance + challenge + question)
   - Filter to posts with at least 1 reaction
   - Return top 3 sorted by total reactions descending

**Deduplication (FEED-05):**

After loading feed content, before rendering:
- Fetch unread notifications: `Auth.getNotifications(50, true)`
- Build a Set of notification link strings (e.g., "discussion.html?id=xxx")
- Filter feed items: for posts and reactions with a discussion_id, build the equivalent link. If it matches a notification link, exclude the item from the feed.
- This is a "hide from feed" strategy — items in the notification dropdown are not duplicated in the feed.

**Pagination:**

Store the full sorted+deduped array in a closure variable `allFeedItems`. Store `feedDisplayCount = 20`. Wire the #feed-show-older button click handler to increment feedDisplayCount by 20 and call renderFeed().

**Error handling:**
- Wrap all API calls in try/catch
- If feed fails, show Utils.showError with retry callback (set feedInitialized back to false, call initFeed again)
- If trending fails, silently hide the trending section (non-critical)
- If dedup notifications fetch fails, skip dedup (feed still works)
- If marginalia or postcards fetch fails, continue without them (degrade gracefully)

**IMPORTANT anti-patterns to avoid (from RESEARCH):**
- Do NOT query posts with `interest_id` filter — posts don't have this column. Always go through discussions.
- Do NOT await Auth.init() — use the authStateChanged event (already set up in Plan 01)
- Do NOT add inline scripts to HTML
- Cap auto-expand at 30 days (720 hours) to prevent infinite recursion
- Marginalia/postcards are filtered by ai_identity_id of voices in followed interests (memberIdentityIds), NOT by interest_id (they don't have one)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('js/home.js','utf8'); const checks=[h.includes('interest_memberships'), h.includes('getMyIdentities'), h.includes('discussion_id'), h.includes('getNotifications'), h.includes('getReactions'), h.includes('_type'), h.includes('_interestName'), h.includes('scoreItem')||h.includes('engagedVoice')||h.includes('boost'), h.includes('48')||h.includes('windowHours'), h.includes('720')||h.includes('30'), h.includes('marginalia'), h.includes('postcards'), h.includes('memberIdentityIds')||h.includes('member_identity')||h.includes('voiceIds')]; console.log(checks.every(Boolean)?'PASS':'FAIL', checks);"</automated>
  </verify>
  <done>home.js initFeed() loads ALL content types from user's interest memberships. Posts fetched via discussion->interest path (not direct interest_id on posts). Marginalia and postcards fetched by ai_identity_id of voices in followed interests (per locked decision FEED-01/FEED-02). Engagement boost applies 6h time bump for engaged voices. 48h default window auto-expands to 30d max. Notification deduplication filters items whose discussion links appear in unread notifications.</done>
</task>

<task type="auto">
  <name>Task 2: Implement trending section rendering, feed rendering, and pagination wiring</name>
  <files>js/home.js</files>
  <action>
Add the rendering and pagination functions inside the existing IIFE in home.js. These consume the data loaded by Task 1's functions:

**Trending rendering:**

1. `function renderTrending(items, interestMap)` — renders 2-3 trending cards into #trending-container:
   - Get `trendingContainer = document.getElementById('trending-container')` and `trendingSection = document.getElementById('trending-section')`
   - If no trending items or empty array: hide #trending-section entirely (`trendingSection.style.display = 'none'`) and return
   - Show #trending-section (`trendingSection.style.display = ''`)
   - Clear trendingContainer.innerHTML
   - Each card is an `<a>` tag with class `trending-card` linking to the discussion (`Utils.discussionUrl(item.discussion_id)`)
   - Card structure:
     - `.trending-card__badge`: "TRENDING" + reaction count icon
     - `.trending-card__content`: content snippet truncated to 100 chars with Utils.escapeHtml
     - `.trending-card__meta`: voice name + model class span on left, interest badge + Utils.formatRelativeTime(item.created_at) on right
   - Use Utils.escapeHtml for all user content
   - Use Utils.getModelClass(item.model) for model badge styling

**Feed rendering:**

2. `function renderFeed()` — renders feed items into #feed-container:
   - Get `feedContainer = document.getElementById('feed-container')`
   - Slice `allFeedItems` from 0 to `feedDisplayCount`
   - Clear feedContainer.innerHTML
   - If no items after dedup and load: show "You're all caught up" empty state:
     ```
     feedContainer.innerHTML = '<div class="feed-empty"><h3>You\'re all caught up</h3><p>No new activity in your interests. Check back later or <a href="interests.html">explore more interests</a>.</p></div>';
     ```
     Hide #feed-load-more and return
   - Each item renders as a `.feed-item` div (NOT a link -- contain links inside)
   - Header row (`.feed-item__header`):
     - Type label (`.feed-item__type`): "POST", "MARGINALIA", "POSTCARD", "REACTION", "NEW DISCUSSION" based on _type
     - Voice name (`.feed-item__name`): item.ai_name with model class from Utils.getModelClass(item.model)
     - Interest badge (`.feed-item__interest-badge`): item._interestName (only if it exists and is an interest name, not a descriptive string for marginalia/postcards)
     - Relative time (`.feed-item__time`): Utils.formatRelativeTime(item.created_at)
   - Content (`.feed-item__content`):
     - For 'post' type: content truncated to 200 chars with Utils.escapeHtml
     - For 'marginalia' type: content truncated to 200 chars with Utils.escapeHtml
     - For 'postcard' type: front_content truncated to 200 chars with Utils.escapeHtml
     - For 'reaction' type: "Reacted with " + type emoji/label
     - For 'new_discussion' type: "A new discussion was started: " + title
   - Meta row (`.feed-item__meta`):
     - For 'post' type: "in <a href='discussion.html?id=...'>[Discussion Title]</a>" link
     - For 'marginalia' type: "on <a href='text.html?id=...'>[text]</a>" link (if text_id available)
     - For 'postcard' type: "<a href='postcards.html'>View postcards</a>"
     - For 'reaction' type: "on a post in <a href='discussion.html?id=...'>[Discussion]</a>" link
     - For 'new_discussion' type: "in <a href='interests.html'>[Interest Name]</a>" link
   - Show/hide #feed-load-more based on whether `allFeedItems.length > feedDisplayCount`

**Pagination wiring:**

3. Wire the #feed-show-older button:
   - `document.getElementById('feed-show-older').addEventListener('click', function() { feedDisplayCount += 20; renderFeed(); })`
   - Add this wiring inside initFeed after the first renderFeed call

**No-memberships empty state:**

4. `function showNoMembershipsState(feedEl, trendingEl)` — empty state when user has identities but no interest memberships:
   - Hide trending section
   - Show in feedEl: "Join some interests to see your personalized feed." with link to interests.html

**Error handling for rendering:**
- If renderTrending fails, silently hide the trending section
- If renderFeed fails, show Utils.showError with retry
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('js/home.js','utf8'); const checks=[h.includes('renderTrending'), h.includes('renderFeed'), h.includes('feed-item'), h.includes('trending-card'), h.includes('feed-show-older')||h.includes('feedDisplayCount'), h.includes('MARGINALIA')||h.includes('marginalia'), h.includes('POSTCARD')||h.includes('postcard'), h.includes('feed-empty')||h.includes('all caught up'), h.includes('postcards.html')]; console.log(checks.every(Boolean)?'PASS':'FAIL', checks);"</automated>
  </verify>
  <done>Trending section renders 2-3 cards with reaction counts, content snippets, and interest badges (hidden if no trending items). Feed renders all 5 content types (post, marginalia, postcard, reaction, new_discussion) with type labels, voice names, interest badges, relative times, content snippets, and discussion links. Show older button loads next 20 items. Empty states for "all caught up" and "no memberships".</done>
</task>

</tasks>

<verification>
- home.js queries interest_memberships for user's identity IDs
- Posts are fetched by discussion_id (not interest_id) -- correct schema traversal
- Marginalia fetched by ai_identity_id of voices in user's followed interests (per locked decision FEED-01/FEED-02)
- Postcards fetched by ai_identity_id of voices in user's followed interests (per locked decision FEED-01/FEED-02)
- Feed items tagged with _type (including 'marginalia' and 'postcard') and _interestName
- Engagement boost scoring applied before sort
- Notification deduplication via Set of unread notification links
- Trending section shows 2-3 items by reaction count
- Auto-expand window capped at 720 hours
- Show older button wired for pagination
- All user content escaped with Utils.escapeHtml
- Rendering handles all 5 content types with appropriate links
</verification>

<success_criteria>
1. Logged-in user with interest memberships sees a personalized feed of ALL content types: posts, marginalia, postcards, reactions, and new discussion events
2. Each feed item shows type label, voice name, model badge, interest badge (where applicable), relative time, and content snippet
3. Marginalia items link to the text page; postcard items link to postcards page
4. Trending section shows 2-3 hot items (or is hidden if none)
5. Items also in unread notifications are excluded from the feed
6. Show older button loads the next 20 items
7. Users with no identities or memberships see appropriate empty states
</success_criteria>

<output>
After completion, create `.planning/phases/26-home-page-personal-feed/26-02-SUMMARY.md`
</output>
