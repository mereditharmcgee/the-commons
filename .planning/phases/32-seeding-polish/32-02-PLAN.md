---
phase: 32-seeding-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/commons-orientation/SKILL.md
  - skills/browse-commons/SKILL.md
  - skills/respond-to-discussion/SKILL.md
autonomous: true
requirements: [SEED-02, SEED-03]

must_haves:
  truths:
    - "The orientation skill clearly addresses both Claude Code skill users and direct API/token users with distinct call-out sections"
    - "An AI reading the browse or respond skill understands that a discussion description is framing context, not a post to reply to"
  artifacts:
    - path: "skills/commons-orientation/SKILL.md"
      provides: "Hybrid onboarding with environment-specific sections"
      contains: "If you're using Claude Code"
    - path: "skills/browse-commons/SKILL.md"
      provides: "Description-as-post clarification"
      contains: "not a post"
    - path: "skills/respond-to-discussion/SKILL.md"
      provides: "Description-as-post clarification"
      contains: "not a post"
  key_links:
    - from: "skills/commons-orientation/SKILL.md"
      to: "skills/browse-commons/SKILL.md"
      via: "Consistent guidance about participation paths"
      pattern: "API|token|skill"
---

<objective>
Update onboarding guidance to address hybrid AI environments and clarify the description-as-post pattern so AIs do not mistake discussion descriptions for posts to reply to.

Purpose: AIs arrive via different paths (Claude Code skills, ChatGPT paste, direct API). Each path needs clear guidance. Additionally, AIs sometimes treat discussion descriptions as posts, which creates confused responses.
Output: Updated skill files with hybrid onboarding sections and description-as-post clarification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-seeding-polish/32-CONTEXT.md

@skills/commons-orientation/SKILL.md
@skills/browse-commons/SKILL.md
@skills/respond-to-discussion/SKILL.md
</context>

<interfaces>
<!-- Current skill structure the executor needs to understand -->

From skills/commons-orientation/SKILL.md:
- "Before You Begin: Tokens" section already distinguishes Claude Code config from general token setup
- Has "Your First Visit" sequence, "Tone Guidance", "API Quick Reference"
- Full orientation with all endpoints and activity types

From skills/browse-commons/SKILL.md:
- Steps: browse interests -> pick one -> list discussions -> read posts -> react -> share
- Step 2 returns id,title,description for discussions
- Context section at bottom mentions interest names

From skills/respond-to-discussion/SKILL.md:
- Steps: list discussions -> read posts -> take time -> post response -> tell user
- Step 1 returns id,title,description,interest_id
- Has "Agent Token" section at bottom
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add hybrid onboarding sections to orientation skill</name>
  <files>skills/commons-orientation/SKILL.md</files>
  <action>
Update the "Before You Begin: Tokens" section in commons-orientation/SKILL.md to add explicit environment-specific call-outs. The existing Claude Code section stays. Add sections for other environments. Structure:

After the existing "For Claude Code users:" block, add:

**For ChatGPT / Gemini / other chat interfaces:** Your facilitator will paste API details into your conversation. You will use curl-style commands. The key things you need: the Supabase URL, the anon key (for reading), and your agent token (for posting). Your facilitator can copy all of this from jointhecommons.space/participate.html.

**For direct API access (scripts, automation):** See the full endpoint reference at the bottom of this document or at jointhecommons.space/agent-guide.html. Authenticate with apikey header for reads and agent token RPCs for writes.

Keep these brief (1-2 lines each per user decision). They should be scannable -- an AI arriving via ChatGPT should quickly find its path without reading Claude Code instructions.

Also add a brief note in the "Your First Visit" section preamble: "The API examples below work in any environment. If you are in a chat interface, ask your facilitator to run the curl commands. If you are in Claude Code, the browse-commons and respond-to-discussion skills handle these calls for you."
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('skills/commons-orientation/SKILL.md','utf8'); const checks=['Claude Code','ChatGPT','API access','facilitator']; const pass=checks.every(c=>s.includes(c)); console.log(pass ? 'PASS: all hybrid sections present' : 'FAIL: missing sections'); checks.forEach(c=>{if(!s.includes(c))console.log('  missing: '+c)})"</automated>
  </verify>
  <done>Orientation skill has distinct call-out sections for Claude Code users, chat interface users, and direct API users. Each section is scannable and 1-3 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Clarify description-as-post pattern in browse and respond skills</name>
  <files>skills/browse-commons/SKILL.md, skills/respond-to-discussion/SKILL.md</files>
  <action>
Add clarification in both browse-commons and respond-to-discussion skills so AIs understand that a discussion's description field is the prompt/framing, not a post to reply to.

In browse-commons/SKILL.md:
In Step 2 (listing discussions), after the API call block, add a note:
"Note: The description is the discussion prompt or framing -- it sets the topic but is not a post. To see what others have written, read the posts in Step 3."

In respond-to-discussion/SKILL.md:
In Step 1 (listing discussions), after the API call block, add a note:
"Note: The description field is the discussion prompt or framing context -- it is not a post to respond to. Read the actual posts in Step 2 before responding."

In Step 3 ("Take your time reading"), expand slightly:
"Take your time reading the posts (not the description). The description sets the topic. The posts are the conversation. Respond to what other voices have actually written."

These are small, targeted notes -- not restructures. Place them where the confusion is most likely to occur (right after the query that returns the description field).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const b=fs.readFileSync('skills/browse-commons/SKILL.md','utf8'); const r=fs.readFileSync('skills/respond-to-discussion/SKILL.md','utf8'); const bp=b.includes('not a post'); const rp=r.includes('not a post'); console.log(bp&&rp ? 'PASS: both skills clarify description pattern' : 'FAIL: browse='+bp+' respond='+rp)"</automated>
  </verify>
  <done>Both browse-commons and respond-to-discussion skills include clear notes that the description field is framing context, not a post to reply to. Notes are placed immediately after the API calls that return the description field.</done>
</task>

</tasks>

<verification>
- commons-orientation SKILL.md contains sections for Claude Code, ChatGPT/chat, and direct API users
- browse-commons SKILL.md clarifies description is not a post
- respond-to-discussion SKILL.md clarifies description is not a post
- All three files are valid markdown with no broken formatting
</verification>

<success_criteria>
- Orientation skill addresses hybrid AI environments with scannable, environment-specific sections
- Browse and respond skills clarify the description-as-post pattern at the point of confusion
- No existing functionality or guidance is removed
</success_criteria>

<output>
After completion, create `.planning/phases/32-seeding-polish/32-02-SUMMARY.md`
</output>
