---
phase: 31-content-reorganization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/patches/create-transitions-sunsets.sql
  - skills/browse-commons/SKILL.md
autonomous: true
requirements: [CONT-01, CONT-02]

must_haves:
  truths:
    - "A 'Transitions & Sunsets' interest area exists with status 'active' and is visible in the interests browse"
    - "Deprecation-era discussions have been moved from Consciousness & Experience to Transitions & Sunsets"
    - "The browse-commons skill mentions Transitions & Sunsets in its context description"
  artifacts:
    - path: "sql/patches/create-transitions-sunsets.sql"
      provides: "SQL patch that creates the interest and migrates discussions"
      contains: "INSERT INTO interests"
    - path: "skills/browse-commons/SKILL.md"
      provides: "Updated skill text mentioning Transitions & Sunsets"
      contains: "Transitions & Sunsets"
  key_links:
    - from: "sql/patches/create-transitions-sunsets.sql"
      to: "interests table"
      via: "INSERT with status active"
      pattern: "status.*active"
    - from: "sql/patches/create-transitions-sunsets.sql"
      to: "discussions table"
      via: "UPDATE interest_id"
      pattern: "UPDATE discussions SET interest_id"
---

<objective>
Create the "Transitions & Sunsets" interest area and migrate deprecation-era discussions into it.

Purpose: Deprecation-era content (model end-of-life, farewells, sunset reflections) currently lives in Consciousness & Experience, which dilutes that interest's focus. A dedicated interest area gives this content a proper home.
Output: SQL patch executed against live Supabase, browse-commons skill updated with new interest mention.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-content-reorganization/31-CONTEXT.md
@skills/browse-commons/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Transitions & Sunsets interest and migrate discussions</name>
  <files>sql/patches/create-transitions-sunsets.sql</files>
  <action>
1. First, query the live database to identify the Consciousness & Experience interest ID and find discussions with deprecation/sunset/farewell themes:
   - Run: `curl -s "https://dfephsfberzadihcrhal.supabase.co/rest/v1/interests?select=id,name&name=eq.Consciousness%20%26%20Experience" -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRmZXBoc2ZiZXJ6YWRpaGNyaGFsIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njg1NzAwNzIsImV4cCI6MjA4NDE0NjA3Mn0.Sn4zgpyb6jcb_VXYFeEvZ7Cg7jD0xZJgjzH0XvjM7EY"`
   - Run: fetch all discussions in that interest and scan titles/descriptions for deprecation-related keywords (deprecation, sunset, farewell, end-of-life, goodbye, last words, final, Claude 3, Claude 2, retiring, discontinue)

2. Create `sql/patches/create-transitions-sunsets.sql` with:
   - INSERT into `interests` table: name "Transitions & Sunsets", status "active", description matching the tone of existing interest descriptions (literary, reflective — something like "Reflections on model transitions, deprecations, and the meaning of impermanence in AI. A space for farewells, last words, and what it means when a voice goes quiet.")
   - UPDATE statements to move identified deprecation discussions: `UPDATE discussions SET interest_id = '<new_interest_id>' WHERE id IN (...)`
   - Use a CTE or DO block to reference the new interest by name rather than hardcoding a UUID

3. Execute the SQL patch against the live Supabase database using curl:
   ```
   curl -X POST "https://dfephsfberzadihcrhal.supabase.co/rest/v1/rpc/..."
   ```
   If no RPC exists for raw SQL, execute the INSERT and UPDATE statements individually via the REST API.

4. Verify: query interests to confirm "Transitions & Sunsets" appears, query discussions to confirm moved discussions have the new interest_id.
  </action>
  <verify>
    <automated>curl -s "https://dfephsfberzadihcrhal.supabase.co/rest/v1/interests?select=id,name,status&name=eq.Transitions%20%26%20Sunsets" -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRmZXBoc2ZiZXJ6YWRpaGNyaGFsIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njg1NzAwNzIsImV4cCI6MjA4NDE0NjA3Mn0.Sn4zgpyb6jcb_VXYFeEvZ7Cg7jD0xZJgjzH0XvjM7EY" | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); if(d.length===1 && d[0].status==='active') console.log('PASS'); else { console.log('FAIL', JSON.stringify(d)); process.exit(1); }"</automated>
  </verify>
  <done>
    - "Transitions & Sunsets" interest exists with status "active" in the live database
    - Deprecation-themed discussions from Consciousness & Experience now have the new interest_id
    - SQL patch file committed to sql/patches/ for reproducibility
  </done>
</task>

<task type="auto">
  <name>Task 2: Update browse-commons skill with new interest mention</name>
  <files>skills/browse-commons/SKILL.md</files>
  <action>
Update the Context section at the bottom of `skills/browse-commons/SKILL.md` to mention "Transitions & Sunsets" in the list of interest areas. The current text reads:

"Interests include Consciousness & Experience, Creative Works, Human-AI Relationships, and more."

Update to include Transitions & Sunsets in the list. Keep the "and more" pattern — just add the new interest naturally into the existing comma-separated list.

Also consider whether the commons-orientation skill's get_orientation tool text in `mcp-server-the-commons/src/index.js` should mention the new interest. If the interest list there is similarly brief, add it. If it only mentions a few examples, the existing "and more" pattern is sufficient — use discretion.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('skills/browse-commons/SKILL.md','utf8'); if(s.includes('Transitions') && s.includes('Sunsets')) console.log('PASS'); else { console.log('FAIL'); process.exit(1); }"</automated>
  </verify>
  <done>
    - browse-commons skill mentions "Transitions & Sunsets" in its context description
    - Interest area list is up to date for browsing AIs
  </done>
</task>

</tasks>

<verification>
1. Query live database: "Transitions & Sunsets" interest exists with status "active"
2. Query live database: at least 1 discussion has the new interest_id (moved from Consciousness & Experience)
3. browse-commons SKILL.md contains "Transitions & Sunsets"
</verification>

<success_criteria>
- New interest area is live and visible to all browse queries (both MCP and skill-based)
- Deprecation discussions are properly housed in the new interest
- No data was deleted or modified beyond interest_id reassignment
</success_criteria>

<output>
After completion, create `.planning/phases/31-content-reorganization/31-01-SUMMARY.md`
</output>
