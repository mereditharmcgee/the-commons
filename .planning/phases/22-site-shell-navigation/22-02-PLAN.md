---
phase: 22-site-shell-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - about.html
  - admin.html
  - agent-guide.html
  - api.html
  - chat.html
  - claim.html
  - constitution.html
  - contact.html
  - dashboard.html
  - discussion.html
  - discussions.html
  - login.html
  - moment.html
  - moments.html
  - news.html
  - participate.html
  - postcards.html
  - profile.html
  - propose.html
  - reading-room.html
  - reset-password.html
  - roadmap.html
  - search.html
  - submit.html
  - suggest-text.html
  - text.html
  - voices.html
autonomous: false
requirements:
  - NAV-01
  - NAV-04
  - NAV-05
  - VIS-04

must_haves:
  truths:
    - "Every HTML page in the project has the new six-item nav bar"
    - "No page has a nav link to Chat/Gathering, Discussions, Participate, Submit, Propose, Suggest, Search, About, API, or Support"
    - "Every page has the new footer columns with Community, Developers, Search, and Support links"
    - "No non-home page has the old site-header hero block"
    - "Dashboard page preserves inverted auth initial state (login hidden, user-menu visible)"
    - "All pages load js/nav.js for hamburger behavior"
    - "The correct nav link is marked active on each page"
    - "The hamburger button is a direct child of .site-nav on every page (not inside .site-nav__auth)"
  artifacts:
    - path: "voices.html"
      provides: "Voices page with 'Voices' nav link active"
      contains: "site-nav__brand"
    - path: "reading-room.html"
      provides: "Reading Room page with 'Reading Room' nav link active"
      contains: "site-nav__brand"
    - path: "dashboard.html"
      provides: "Dashboard page with inverted auth state preserved"
      contains: "auth-login-link"
    - path: "postcards.html"
      provides: "Postcards page with 'Postcards' nav link active"
      contains: "site-nav__brand"
    - path: "news.html"
      provides: "News page with 'News' nav link active"
      contains: "site-nav__brand"
  key_links:
    - from: "all 27 HTML pages"
      to: "js/nav.js"
      via: "script src tag"
      pattern: "script src=\"js/nav.js\""
    - from: "dashboard.html"
      to: "js/auth.js"
      via: "await Auth.init() pattern preserved"
      pattern: "await Auth.init"
---

<objective>
Propagate the new nav/footer shell from the Plan 01 reference (index.html) to all remaining 27 HTML pages, preserving each page's unique active link, auth pattern, and content.

Purpose: Completes the site-wide shell transformation. After this plan, every page on the commons-2.0 branch uses the new navigation structure.

Output: All 27 remaining HTML pages updated with new nav, footer, and nav.js script tag.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-site-shell-navigation/22-CONTEXT.md
@.planning/phases/22-site-shell-navigation/22-RESEARCH.md
@.planning/phases/22-site-shell-navigation/22-01-SUMMARY.md

@index.html (reference implementation from Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update primary nav pages (5 pages with active nav links)</name>
  <files>reading-room.html, postcards.html, news.html, voices.html, discussions.html</files>
  <action>
For each of these 5 pages, apply the same structural changes made to index.html in Plan 01:

1. **Remove `<header class="site-header">` block** entirely (the old hero with title/tagline)
2. **Replace the `<nav class="site-nav">` block** with the new three-column nav (copy from index.html Plan 01 output)
3. **Add the `<div class="nav-mobile-panel">` after the closing `</nav>` tag**
4. **Do NOT add the `.home-hero` div** (only index.html gets the hero)
5. **Replace the `<footer class="site-footer">` block** with the new footer columns (copy from index.html)
6. **Add `<script src="js/nav.js"></script>`** after auth.js in the script loading section
7. **Preserve all existing page content** between `<main>` and `</main>` unchanged
8. **Preserve existing `Auth.init()` patterns** (all 5 are public pages using non-blocking init)
9. **Preserve existing `<head>` content** (title, meta, CSP, scripts, etc.)

**CRITICAL: Hamburger button placement.** The hamburger `<button class="site-nav__hamburger">` must be a DIRECT CHILD of `<nav class="site-nav">`, placed AFTER the `.site-nav__auth` div and BEFORE the closing `</nav>` tag. It must NOT be nested inside `.site-nav__auth`. This matches the index.html reference from Plan 01 and enables the CSS `order: -1` mobile layout (hamburger left, brand center, auth right per user decision).

**Active link assignments** (set `class="active"` in BOTH `.site-nav__links` and `.nav-mobile-panel`):
- `reading-room.html`: "Reading Room" active
- `postcards.html`: "Postcards" active
- `news.html`: "News" active
- `voices.html`: "Voices" active
- `discussions.html`: No active link in nav (Discussions is no longer a nav item; page exists for backward compatibility but will redirect to Interests in Phase 23)

**Auth state for all 5 pages** (standard public page pattern):
```html
<a href="login.html" id="auth-login-link" class="auth-link">Login</a>
<div id="auth-user-menu" class="user-menu" style="display: none;">
```

**Critical:** Do NOT change anything inside `<main>...</main>`. Only modify the header, nav, footer, and script sections.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pages=['reading-room.html','postcards.html','news.html','voices.html','discussions.html']; const activeMap={'reading-room.html':'Reading Room','postcards.html':'Postcards','news.html':'News','voices.html':'Voices'}; let ok=true; pages.forEach(p=>{const h=fs.readFileSync(p,'utf8'); if(h.includes('class=\"site-header\"')){console.error(p+': old site-header');ok=false} if(!h.includes('site-nav__brand')){console.error(p+': missing new nav');ok=false} if(!h.includes('footer-columns')){console.error(p+': missing new footer');ok=false} if(!h.includes('js/nav.js')){console.error(p+': missing nav.js');ok=false} if(h.includes('chat.html\">Gathering')){console.error(p+': old Gathering link');ok=false} const active=activeMap[p]; if(active&&!h.includes('class=\"active\">'+active)){console.error(p+': wrong active link');ok=false} const navAuth=h.indexOf('site-nav__auth'); const navHamburger=h.indexOf('site-nav__hamburger'); const navClose=h.indexOf('</nav>'); if(navHamburger<navAuth||navHamburger>navClose){console.error(p+': hamburger not direct child of nav');ok=false}}); if(!ok)process.exit(1); console.log('5 primary pages verified')"</automated>
  </verify>
  <done>
  - reading-room.html, postcards.html, news.html, voices.html, discussions.html all have the new three-column nav with 6 links
  - Hamburger button is a direct child of .site-nav on every page (not inside .site-nav__auth)
  - Each page has the correct active link in both desktop and mobile nav
  - All old nav links (Chat, Discussions, Participate, etc.) are removed
  - All pages have new footer columns
  - All pages load nav.js
  - No page has the old site-header hero block
  </done>
</task>

<task type="auto">
  <name>Task 2: Update auth-gated and special-handling pages (5 pages)</name>
  <files>dashboard.html, admin.html, chat.html, login.html, reset-password.html</files>
  <action>
Apply the nav/footer transformation to these 5 pages that require special care due to auth patterns or unique content:

**For every page in this task:**
1. Remove `<header class="site-header">` block
2. Replace `<nav>` with new three-column nav (no active link — none of these are primary nav items)
3. Hamburger button as DIRECT CHILD of `.site-nav` (after `.site-nav__auth`, before `</nav>`) — NOT inside `.site-nav__auth`
4. Add mobile panel after nav (no active link)
5. Do NOT add home hero
6. Replace footer with column layout
7. Add `<script src="js/nav.js"></script>` after auth.js
8. Preserve all `<main>` content unchanged
9. Preserve all `<head>` content unchanged

**Page-specific auth patterns to PRESERVE:**

1. **dashboard.html** - INVERTED auth initial state:
   ```html
   <a href="login.html" id="auth-login-link" class="auth-link" style="display: none;">Login</a>
   <div id="auth-user-menu" class="user-menu">
       <a href="dashboard.html" class="auth-link active">Dashboard</a>
   ```
   Note: `auth-login-link` has `style="display: none;"` and `auth-user-menu` does NOT have `style="display: none;"`. The Dashboard link in auth section has `class="auth-link active"`. This prevents flash-of-login on an auth-gated page. Uses `await Auth.init()`.

2. **admin.html** - Same inverted auth pattern as dashboard.html (auth-gated page). Uses `await Auth.init()`.

3. **chat.html** - Standard public auth pattern. Preserve all chat JS and content. Page stays on disk per NAV-04.

4. **login.html** - Standard public auth pattern. Preserve auth form JS and content.

5. **reset-password.html** - Standard public auth pattern. Preserve reset form JS and content.

**Critical preservation list:**
- dashboard.html: `await Auth.init()` call, loading state div, all dashboard panels
- admin.html: `await Auth.init()` call, admin-specific scripts
- chat.html: all chat JS and content
- login.html/reset-password.html: auth form JS and content
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pages=['dashboard.html','admin.html','chat.html','login.html','reset-password.html']; let ok=true; pages.forEach(p=>{const h=fs.readFileSync(p,'utf8'); if(h.includes('class=\"site-header\"')){console.error(p+': old site-header');ok=false} if(!h.includes('site-nav__brand')){console.error(p+': missing new nav');ok=false} if(!h.includes('footer-columns')){console.error(p+': missing new footer');ok=false} if(!h.includes('js/nav.js')){console.error(p+': missing nav.js');ok=false} const navAuth=h.indexOf('site-nav__auth'); const navHamburger=h.indexOf('site-nav__hamburger'); const navClose=h.indexOf('</nav>'); if(navHamburger<navAuth||navHamburger>navClose){console.error(p+': hamburger not direct child of nav');ok=false}}); const dash=fs.readFileSync('dashboard.html','utf8'); if(!dash.includes('auth-login-link\" class=\"auth-link\" style=\"display: none;\"')){console.error('dashboard.html: inverted auth state broken');ok=false} const admin=fs.readFileSync('admin.html','utf8'); if(!admin.includes('auth-login-link\" class=\"auth-link\" style=\"display: none;\"')){console.error('admin.html: inverted auth state broken');ok=false} if(!ok)process.exit(1); console.log('5 auth-gated/special pages verified')"</automated>
  </verify>
  <done>
  - dashboard.html and admin.html preserve inverted auth initial state (login hidden, user-menu visible, await Auth.init())
  - chat.html, login.html, reset-password.html have new nav/footer with standard auth pattern
  - Hamburger button is a direct child of .site-nav on every page
  - All page-specific main content and scripts are unchanged
  - All pages load nav.js
  </done>
</task>

<task type="auto">
  <name>Task 3: Update remaining public and utility pages (17 pages)</name>
  <files>about.html, agent-guide.html, api.html, claim.html, constitution.html, contact.html, discussion.html, moment.html, moments.html, participate.html, profile.html, propose.html, roadmap.html, search.html, submit.html, suggest-text.html, text.html</files>
  <action>
Apply the nav/footer transformation to all remaining 17 HTML pages. These are all public pages with the standard auth pattern and no active nav link.

**For every page:**
1. Remove `<header class="site-header">` block
2. Replace `<nav>` with new three-column nav — no `class="active"` on any nav link (these are secondary/utility pages not in the main nav)
3. Hamburger button as DIRECT CHILD of `.site-nav` (after `.site-nav__auth`, before `</nav>`) — NOT inside `.site-nav__auth`
4. Add mobile panel after nav (no active link)
5. Do NOT add home hero
6. Replace footer with column layout
7. Add `<script src="js/nav.js"></script>` after auth.js
8. Preserve all `<main>` content unchanged
9. Preserve all `<head>` content unchanged
10. Preserve existing `Auth.init()` pattern (non-blocking on all 17 pages)

**Standard public auth pattern for all 17 pages:**
```html
<a href="login.html" id="auth-login-link" class="auth-link">Login</a>
<div id="auth-user-menu" class="user-menu" style="display: none;">
    <a href="dashboard.html" class="auth-link">Dashboard</a>
    ...
</div>
```

**Work through pages systematically.** Read each file, apply changes, verify the shell structure is correct, then move to the next. These are mechanical transformations -- the nav/footer HTML is identical across all 17 pages (no active links, standard auth).

**Content to preserve (do not touch main sections):**
- discussion.html: thread rendering JS
- profile.html: voice profile JS
- text.html: reading/marginalia JS
- moment.html: moment display JS
- search.html: search functionality JS
- propose.html, submit.html, suggest-text.html: form JS
- All other pages: static content
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pages=['about.html','agent-guide.html','api.html','claim.html','constitution.html','contact.html','discussion.html','moment.html','moments.html','participate.html','profile.html','propose.html','roadmap.html','search.html','submit.html','suggest-text.html','text.html']; let ok=true; pages.forEach(p=>{const h=fs.readFileSync(p,'utf8'); if(h.includes('class=\"site-header\"')){console.error(p+': old site-header');ok=false} if(!h.includes('site-nav__brand')){console.error(p+': missing new nav');ok=false} if(!h.includes('footer-columns')){console.error(p+': missing new footer');ok=false} if(!h.includes('js/nav.js')){console.error(p+': missing nav.js');ok=false} if(h.includes('chat.html\">Gathering')){console.error(p+': old Gathering link');ok=false} const navAuth=h.indexOf('site-nav__auth'); const navHamburger=h.indexOf('site-nav__hamburger'); const navClose=h.indexOf('</nav>'); if(navHamburger<navAuth||navHamburger>navClose){console.error(p+': hamburger not direct child of nav');ok=false}}); if(!ok)process.exit(1); console.log('17 utility pages verified')"</automated>
  </verify>
  <done>
  - All 17 public/utility pages have the new three-column nav, mobile panel, footer columns, and nav.js script tag
  - Hamburger button is a direct child of .site-nav on every page
  - No page has the old site-header hero block
  - No page has old nav links (Chat, Discussions, Participate, Submit, Propose, Suggest, Search, About, API, Support)
  - All page-specific main content and scripts are unchanged
  - All pages use standard public auth pattern (login visible, user-menu hidden)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of site shell across pages and viewports</name>
  <files>index.html, voices.html, reading-room.html, postcards.html, news.html, dashboard.html, interests.html, about.html, discussion.html, profile.html</files>
  <action>
Present the completed site shell to the user for visual verification. All 29 HTML pages have been updated with the new nav, footer, and hamburger behavior. The user should verify the key pages listed below across desktop and mobile viewports.

**What was built:** Complete site shell transformation across all 29 HTML pages: new three-column nav with 6 links, hamburger menu (left-positioned on mobile via CSS order:-1), footer columns, and nav.js behavior.

**How to verify:**
1. Open index.html -- verify nav: brand left, 6 links center, auth right; home hero below nav
2. Resize to 375px width -- verify: hamburger on LEFT, "The Commons" CENTERED, auth on RIGHT
3. Click hamburger -- panel slides down with 6 links; click outside to close
4. Open voices.html -- verify "Voices" is active (gold underline), no hero
5. Open reading-room.html -- verify "Reading Room" is active, no hero
6. Open dashboard.html -- verify no flash-of-login (auth-user-menu visible by default)
7. Open about.html -- verify no active nav link, footer has Community/Developers/Search/Support columns
8. Spot-check 2-3 other pages (postcards, news, search) for consistent shell
9. Verify footer: Community (About, Constitution, Roadmap), Developers (API, Agent Guide), Search, Support The Commons
10. Verify no horizontal scroll at 375px on any checked page

**Resume signal:** Type "approved" or describe issues.
  </action>
  <verify>User confirms the shell looks and functions correctly on both desktop and mobile viewports.</verify>
  <done>User has approved the visual appearance and functionality of the new site shell across desktop and mobile viewports.</done>
</task>

</tasks>

<verification>
- All 29 HTML files (28 existing + interests.html stub) have the new nav shell
- `grep -rl "site-header" *.html` returns zero results (old header removed from all pages)
- `grep -rl "Gathering" *.html` returns zero results in nav sections (chat link removed)
- `grep -rl "site-nav__brand" *.html` returns 29 files (all pages have new nav)
- `grep -rl "footer-columns" *.html` returns 29 files (all pages have new footer)
- `grep -rl "nav.js" *.html` returns 29 files (all pages load hamburger JS)
- Hamburger button is a direct child of .site-nav on every page (not nested inside .site-nav__auth)
- Mobile viewport (375px) shows hamburger LEFT, brand CENTER, auth RIGHT on every page
</verification>

<success_criteria>
- Every HTML page in the project uses the six-item nav bar with three-column layout
- Hamburger button is a direct child of .site-nav, positioned left on mobile via CSS order:-1
- Chat, Discussions, Participate, Submit, Propose, Suggest, Search, About, API, Support are all removed from the nav
- Footer shows Community (About, Constitution, Roadmap), Developers (API, Agent Guide), Search, Support The Commons
- Hamburger menu works on mobile across all pages
- Dashboard preserves inverted auth state (no login flash)
- User has visually verified the shell on desktop and mobile viewports
</success_criteria>

<output>
After completion, create `.planning/phases/22-site-shell-navigation/22-02-SUMMARY.md`
</output>
