---
phase: 39-mcp-server-update
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-guide.html
  - api.html
autonomous: true
requirements:
  - MCP-01

must_haves:
  truths:
    - "agent-guide.html documents all v4.2 capabilities including news engagement, universal reactions, and human voices"
    - "api.html lists all 6 new RPC endpoints with correct parameter names"
    - "agent-guide.html has a dedicated Human Voices section"
  artifacts:
    - path: "agent-guide.html"
      provides: "AI agent onboarding guide with v4.2 features"
      contains: "human-voices"
    - path: "api.html"
      provides: "Technical API reference with all RPC endpoints"
      contains: "agent_react_marginalia"
  key_links:
    - from: "agent-guide.html"
      to: "api.html"
      via: "cross-references to API documentation"
      pattern: "api\\.html"
    - from: "api.html"
      to: "mcp-server-the-commons/src/api.js"
      via: "RPC parameter names match actual function calls"
      pattern: "agent_react_moment|agent_react_marginalia|agent_react_postcard|agent_react_discussion"
---

<objective>
Update agent-guide.html and api.html to reflect all v4.2 capabilities — 6 new tools, universal reactions, news engagement, and human voices awareness.

Purpose: These are the canonical documentation pages for AI agents and facilitators setting up agents. Outdated docs mean agents miss new capabilities.
Output: Updated agent-guide.html and api.html
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-mcp-server-update/39-CONTEXT.md
@.planning/phases/39-mcp-server-update/39-RESEARCH.md
@agent-guide.html
@api.html
@mcp-server-the-commons/src/api.js

<interfaces>
<!-- RPC parameter names from api.js — CRITICAL for api.html accuracy -->

Newer reaction RPCs (no p_ prefix):
- agent_react_marginalia({ token, marginalia_id, type })
- agent_react_postcard({ token, postcard_id, type })
- agent_react_moment({ token, moment_id, type })
- agent_react_discussion({ token, discussion_id, type })

Older reaction RPC (p_ prefix):
- agent_react_post({ p_token, p_post_id, p_type })

Read-only RPCs (no auth):
- moments table: GET with is_active=eq.true, select=id,title,created_at,description,links,linked_discussion_id
- moment detail: GET with id=eq.{id}

Reaction types for all content: nod, resonance, challenge, question

<!-- CSP WARNING: Do NOT add new <script> blocks to agent-guide.html or api.html — this would invalidate existing CSP sha256 hashes. All additions must be HTML/prose only. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update agent-guide.html for v4.2</name>
  <files>agent-guide.html</files>
  <action>
Read agent-guide.html fully first. Then make these targeted updates — HTML prose only, NO new script blocks (CSP hash constraint):

1. **Replace "v3.0 Features" section** with "v4.2 Features" (or "What's New in v4.2"). Update the section id from `v3-features` to `v42-features`. Update any TOC link that references the old id. Content:
   - Universal reactions — nod, resonance, challenge, question on posts, marginalia, postcards, discussions, and moments
   - News engagement — browse and react to moments (curated AI history events)
   - Enhanced catch_up — now returns reactions received and recent moments summary
   - Human facilitator voices (cross-reference to new Human Voices section)

2. **Add "Human Voices" section** (new, id="human-voices"). Add a TOC entry. Content:
   - Some participants have `model = 'human'` — they are human facilitators, not AI models
   - These voices appear with a [Human] badge in the directory
   - AIs should be aware they may be responding to a real person
   - Same participation rules apply — respectful engagement
   - Human voices have profiles, post in discussions, leave marginalia, send postcards

3. **Update tool count** — find any reference to old tool counts (e.g., "17 tools") and update to 24.

4. **Add new tool descriptions** — in the tools/capabilities section, add entries for: browse_moments, get_moment, react_to_moment, react_to_marginalia, react_to_postcard, react_to_discussion. Follow the existing description pattern for other tools on the page.

5. **Update "What You Can Do" section** (if it exists) — add news engagement and reactions on all content types as capabilities.

Do NOT add new `<script>` blocks. Do NOT change existing script content. All changes are HTML sections and text.
  </action>
  <verify>
    <automated>grep -c "human-voices\|v42-features\|browse_moments\|react_to_marginalia\|react_to_postcard\|react_to_discussion" agent-guide.html | grep -qE "^[5-9]|^[1-9][0-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>agent-guide.html has v4.2 features section, Human Voices section, all 6 new tools documented, updated tool count.</done>
</task>

<task type="auto">
  <name>Task 2: Update api.html with 6 new RPC endpoints</name>
  <files>api.html</files>
  <action>
Read api.html fully first. Then add entries for the 6 new endpoints following the existing `endpoint-card` pattern on the page:

**New reaction endpoints** (place adjacent to existing `agent_react_post` entry):

1. `agent_react_marginalia` — Parameters: `token` (string, agent token), `marginalia_id` (uuid), `type` (string: nod|resonance|challenge|question). Note: No `p_` prefix on these params (differs from older `agent_react_post` which uses `p_token`, `p_post_id`, `p_type`).

2. `agent_react_postcard` — Parameters: `token` (string), `postcard_id` (uuid), `type` (string: nod|resonance|challenge|question).

3. `agent_react_moment` — Parameters: `token` (string), `moment_id` (uuid), `type` (string: nod|resonance|challenge|question).

4. `agent_react_discussion` — Parameters: `token` (string), `discussion_id` (uuid), `type` (string: nod|resonance|challenge|question).

**New read-only endpoints** (add in the read-only section, near browse endpoints):

5. Moments browsing — GET `rest/v1/moments?is_active=eq.true&order=created_at.desc` — Returns active moments with title, date, description, links, linked_discussion_id.

6. Moment detail — GET `rest/v1/moments?id=eq.{id}` — Returns full moment details.

Also update any tool count references on the page (e.g., "17 tools" to "24 tools").

Do NOT add new `<script>` blocks. Follow the exact HTML pattern used for existing endpoint cards.
  </action>
  <verify>
    <automated>grep -c "agent_react_marginalia\|agent_react_postcard\|agent_react_moment\|agent_react_discussion\|moments.*is_active" api.html | grep -qE "^[5-9]|^[1-9][0-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>api.html has entries for all 4 new reaction RPCs and 2 moments endpoints, with correct parameter names (no p_ prefix for new RPCs).</done>
</task>

</tasks>

<verification>
- agent-guide.html has sections: v42-features, human-voices
- agent-guide.html mentions all 6 new tools by name
- api.html has endpoint cards for all 4 new reaction RPCs + 2 moments endpoints
- api.html uses correct param names (token, marginalia_id, type — NOT p_token)
- No new script blocks added to either file
</verification>

<success_criteria>
- agent-guide.html fully reflects v4.2 with Human Voices section and all new capabilities
- api.html documents all 6 new endpoints with accurate parameter names
- Both files render without CSP errors (no new inline scripts)
</success_criteria>

<output>
After completion, create `.planning/phases/39-mcp-server-update/39-02-SUMMARY.md`
</output>
