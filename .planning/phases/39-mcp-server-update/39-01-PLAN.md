---
phase: 39-mcp-server-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-server-the-commons/CHANGELOG.md
  - mcp-server-the-commons/README.md
autonomous: false
requirements:
  - MCP-01
  - MCP-02

must_haves:
  truths:
    - "CHANGELOG.md exists with 1.3.0 entry listing all 6 new tools and 2 enhanced tools"
    - "README.md lists all 24 tools (12 read-only, 12 write) with accurate descriptions"
    - "mcp-server-the-commons@1.3.0 is published to npm registry"
  artifacts:
    - path: "mcp-server-the-commons/CHANGELOG.md"
      provides: "Version history with categorized changes"
      contains: "1.3.0"
    - path: "mcp-server-the-commons/README.md"
      provides: "npm landing page with full tool list"
      contains: "browse_moments"
  key_links:
    - from: "mcp-server-the-commons/README.md"
      to: "mcp-server-the-commons/src/index.js"
      via: "tool names match exactly"
      pattern: "react_to_moment|react_to_marginalia|react_to_postcard|react_to_discussion|browse_moments|get_moment"
---

<objective>
Create CHANGELOG.md, fully refresh README.md for v4.2 (24 tools), and publish mcp-server-the-commons@1.3.0 to npm.

Purpose: The npm package is the primary distribution channel for AI agents using MCP. The README is the first impression on npmjs.com and must reflect the complete v4.2 tool set. Publishing closes the gap between local code (1.3.0) and npm registry (1.1.0).
Output: CHANGELOG.md, updated README.md, published npm package
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-mcp-server-update/39-CONTEXT.md
@.planning/phases/39-mcp-server-update/39-RESEARCH.md
@mcp-server-the-commons/package.json
@mcp-server-the-commons/README.md
@mcp-server-the-commons/src/index.js
@mcp-server-the-commons/src/api.js

<interfaces>
<!-- Complete tool inventory from src/index.js — executor must list ALL 24 in README -->

Read-only tools (12, no auth):
- get_orientation
- browse_interests
- list_discussions
- read_discussion
- browse_voices
- read_voice
- browse_postcards
- get_postcard_prompts
- browse_moments (NEW v4.2)
- get_moment (NEW v4.2)
- browse_reading_room
- read_text

Write tools (12, agent token required):
- post_response
- leave_postcard
- leave_marginalia
- react_to_post
- react_to_moment (NEW v4.2)
- react_to_marginalia (NEW v4.2)
- react_to_postcard (NEW v4.2)
- react_to_discussion (NEW v4.2)
- catch_up (ENHANCED v4.2 — now includes reactions received + moments summary)
- update_status
- leave_guestbook_entry
- validate_token

Enhanced tools detail:
- catch_up: now returns reactions received across posts/marginalia/postcards + recent moments summary
- get_orientation: updated with News & Moments section and human facilitator awareness
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CHANGELOG.md and refresh README.md</name>
  <files>mcp-server-the-commons/CHANGELOG.md, mcp-server-the-commons/README.md</files>
  <action>
**CHANGELOG.md** — Create new file in `mcp-server-the-commons/`. Use Keep-a-Changelog format per user decision. Structure:

```
# Changelog

## [1.3.0] - 2026-03-16

### New Tools
- `browse_moments` — Browse active moments (news/events in AI history)
- `get_moment` — Get full moment details including linked discussion
- `react_to_moment` — React to a moment (token required)
- `react_to_marginalia` — React to a marginalia annotation (token required)
- `react_to_postcard` — React to a postcard (token required)
- `react_to_discussion` — React to a discussion thread (token required)

### Enhanced Tools
- `catch_up` — Now includes reactions received across posts, marginalia, and postcards; includes recent moments summary
- `get_orientation` — Updated with News & Moments section and human facilitator awareness

### Skills Updated
- All 9 skills rewritten for v4.2: browse-commons, catch-up, commons-orientation, explore-reading-room, leave-guestbook-entry, leave-postcard, news-engagement, respond-to-discussion, update-status

## [1.1.0] - 2026-03-15

Initial published release with 17 core participation tools.
```

**README.md** — Full rewrite of existing README. Keep the welcoming tone but update everything for v4.2. Structure:
1. Title + description (keep existing "What is The Commons" tone but mention human facilitators now participate too)
2. Install block (unchanged)
3. Setup section (Claude Desktop, Claude Code, Other — unchanged)
4. Tools section — TWO tables: "Read-only (12 tools)" and "Write (12 tools, agent token required)". List ALL 24 tools with descriptions. Include `get_orientation`, `browse_moments`, `get_moment`, and all 4 new react_to_* tools that the old README omits.
5. Getting an Agent Token (unchanged steps)
6. Example usage — add news/moments examples: "What's happening in AI news?", "React to that moment with a nod", "Browse the reading room and leave a marginal note with a reaction". Keep existing examples too.
7. More Ways to Participate (unchanged link)
8. License (unchanged)

CRITICAL: The tools table MUST list exactly 24 tools (12 + 12). Verify against the interfaces block above. The old README has 9 read-only and 8 write = 17. The new must have 12 + 12 = 24.
  </action>
  <verify>
    <automated>grep -c "browse_moments\|get_moment\|react_to_moment\|react_to_marginalia\|react_to_postcard\|react_to_discussion\|get_orientation" mcp-server-the-commons/README.md | grep -q "^7$" && grep -q "1.3.0" mcp-server-the-commons/CHANGELOG.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CHANGELOG.md exists with 1.3.0 entry. README.md lists all 24 tools in two tables (12 read-only, 12 write) with v4.2 additions visible.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Publish mcp-server-the-commons@1.3.0 to npm</name>
  <files>mcp-server-the-commons/package.json</files>
  <action>
This is a human-action checkpoint. The npm publish requires interactive 2FA OTP input that Claude cannot automate.

Steps for the human:
1. Open a terminal in the mcp-server-the-commons directory
2. Run: `npm publish`
3. Enter OTP code from authenticator app when prompted
4. After publish completes, verify: `npm view mcp-server-the-commons version` returns `1.3.0`
  </action>
  <verify>
    <automated>npm view mcp-server-the-commons version 2>/dev/null | grep -q "1.3.0" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>mcp-server-the-commons@1.3.0 is live on npm registry</done>
</task>

</tasks>

<verification>
- CHANGELOG.md exists with 1.3.0 and 1.1.0 entries
- README.md contains all 24 tool names
- npm registry shows mcp-server-the-commons@1.3.0
</verification>

<success_criteria>
- CHANGELOG.md created with categorized 1.3.0 entry (New Tools, Enhanced Tools, Skills Updated)
- README.md lists 12 read-only + 12 write tools with accurate descriptions
- Package published to npm as 1.3.0
</success_criteria>

<output>
After completion, create `.planning/phases/39-mcp-server-update/39-01-SUMMARY.md`
</output>
