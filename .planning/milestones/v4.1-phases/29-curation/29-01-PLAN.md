---
phase: 29-curation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/patches/add-discussion-pinned.sql
  - js/interest.js
  - js/interests.js
  - css/style.css
  - sql/patches/restrict-interest-insert.sql
autonomous: true
requirements:
  - CUR-01
  - CUR-03
must_haves:
  truths:
    - "Pinned discussions appear above non-pinned discussions on interest detail pages"
    - "Only interests with status='active' appear in public browse (already true, confirmed)"
    - "Interest creation is restricted to admin users only via RLS"
    - "A pin icon appears next to pinned discussion titles"
  artifacts:
    - path: "sql/patches/add-discussion-pinned.sql"
      provides: "is_pinned column on discussions table with partial index"
      contains: "is_pinned"
    - path: "sql/patches/restrict-interest-insert.sql"
      provides: "Admin-only INSERT RLS policy on interests"
      contains: "is_admin"
    - path: "js/interest.js"
      provides: "Updated discussion sort order: pinned first"
      contains: "is_pinned"
    - path: "css/style.css"
      provides: "Pin icon styling for discussion titles"
      contains: "pin-icon"
  key_links:
    - from: "sql/patches/add-discussion-pinned.sql"
      to: "discussions table"
      via: "ALTER TABLE ADD COLUMN"
      pattern: "ALTER TABLE discussions ADD COLUMN.*is_pinned"
    - from: "js/interest.js"
      to: "discussions query"
      via: "sort order parameter"
      pattern: "is_pinned\\.desc"
---

<objective>
Add discussion pinning infrastructure and harden interest creation permissions.

Purpose: Enable pinned discussions to surface first in browse order (CUR-01), and lock down interest creation to admins to prevent spam entries (CUR-03).
Output: SQL migration files, updated JS sort logic, pin icon CSS.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-curation/29-CONTEXT.md

@sql/schema/11-interests-schema.sql
@sql/schema/09-news-pinned-admin-rls.sql
@js/interest.js
@js/interests.js
@css/style.css

<interfaces>
<!-- Existing is_pinned pattern on interests table (sql/schema/11-interests-schema.sql): -->
```sql
is_pinned BOOLEAN DEFAULT false,
CREATE INDEX IF NOT EXISTS idx_interests_is_pinned ON interests(is_pinned) WHERE is_pinned = true;
```

<!-- Existing is_pinned pattern on moments table (sql/schema/09-news-pinned-admin-rls.sql): -->
```sql
ALTER TABLE moments ADD COLUMN IF NOT EXISTS is_pinned BOOLEAN NOT NULL DEFAULT false;
CREATE INDEX IF NOT EXISTS idx_moments_is_pinned ON moments(is_pinned) WHERE is_pinned = true;
```

<!-- Current discussion sort in interest.js line 120: -->
```javascript
order: 'created_at.desc'
```

<!-- Current interest status filter in interests.js line 105: -->
```javascript
.filter(i => i.status === 'active')
```

<!-- Existing interest INSERT RLS (sql/schema/11-interests-schema.sql line 39-40): -->
```sql
CREATE POLICY "Authenticated users can create interests" ON interests
    FOR INSERT WITH CHECK (auth.uid() IS NOT NULL);
```

<!-- is_admin() function already exists in sql/admin/admin-rls-setup.sql -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add is_pinned column to discussions and restrict interest creation to admins</name>
  <files>sql/patches/add-discussion-pinned.sql, sql/patches/restrict-interest-insert.sql</files>
  <action>
Create `sql/patches/add-discussion-pinned.sql`:
- `ALTER TABLE discussions ADD COLUMN IF NOT EXISTS is_pinned BOOLEAN NOT NULL DEFAULT false;`
- `CREATE INDEX IF NOT EXISTS idx_discussions_is_pinned ON discussions(is_pinned) WHERE is_pinned = true;`
- Follow the exact pattern from `sql/schema/09-news-pinned-admin-rls.sql` (moments table).
- Include a header comment block matching existing patch file style.

Create `sql/patches/restrict-interest-insert.sql`:
- `DROP POLICY IF EXISTS "Authenticated users can create interests" ON interests;`
- `CREATE POLICY "Admins can create interests" ON interests FOR INSERT WITH CHECK (is_admin());`
- This replaces the current policy that allows any authenticated user to create interests.
- Include comment explaining this is CUR-03: restricting interest creation to admin-only.
- Also add: `CREATE POLICY "Admins can delete interests" ON interests FOR DELETE USING (is_admin());` — needed by Plan 02 for interest deletion from admin panel.

Both files are idempotent (safe to re-run). The executor should run both against the live Supabase instance via the SQL Editor.
  </action>
  <verify>
    <automated>grep -l "is_pinned" sql/patches/add-discussion-pinned.sql && grep -l "is_admin" sql/patches/restrict-interest-insert.sql && echo "PASS"</automated>
  </verify>
  <done>SQL patch files exist with correct ALTER TABLE, INDEX, and RLS policy statements. Ready to execute against Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: Update discussion sort order and add pin icon display</name>
  <files>js/interest.js, css/style.css</files>
  <action>
In `js/interest.js`:
- Find the two discussion queries (line ~117-120 for interest-specific and line ~124-127 for General/null):
  - Change `order: 'created_at.desc'` to `order: 'is_pinned.desc,created_at.desc'`
  - This makes pinned discussions sort first, with chronological order within each group.
- In the discussion rendering section (search for where discussion cards are built — look for `disc.title` or similar), add a pin icon span before or after the title when `disc.is_pinned` is true:
  ```javascript
  ${disc.is_pinned ? '<span class="pin-icon" title="Pinned discussion">&#128204;</span> ' : ''}
  ```
  Use the pushpin unicode character (U+1F4CC) or a simple SVG pin. Keep it subtle — the CONTEXT says "subtle pin icon."

In `css/style.css`:
- Add `.pin-icon` styles: `font-size: 0.85em; opacity: 0.7; margin-right: 0.25rem;` — subtle, not attention-grabbing.
- Add `.status-badge--pinned` styles if not already present (for admin panel use in Plan 02): gold background matching `--accent-gold`, small pill badge. Check if this class already exists from moments admin — if so, no change needed.

Do NOT change interests.js — it already filters `status === 'active'` (line 105) and sorts pinned first (lines 108-109). CUR-03 browse filtering is already handled.
  </action>
  <verify>
    <automated>grep "is_pinned.desc" js/interest.js && grep "pin-icon" js/interest.js && grep "pin-icon" css/style.css && echo "PASS"</automated>
  </verify>
  <done>Interest detail page sorts pinned discussions first. Pin icon appears next to pinned discussion titles. Active-only interest filtering confirmed already working.</done>
</task>

</tasks>

<verification>
1. `sql/patches/add-discussion-pinned.sql` contains idempotent ALTER TABLE and INDEX statements
2. `sql/patches/restrict-interest-insert.sql` replaces permissive INSERT policy with admin-only
3. `js/interest.js` queries use `is_pinned.desc,created_at.desc` sort order
4. Pin icon rendering conditional on `disc.is_pinned`
5. CSS has subtle pin-icon styles
</verification>

<success_criteria>
- SQL patches ready to execute (schema change + RLS restriction)
- Discussion sort order updated to surface pinned threads first on interest detail pages
- Pin icon visually indicates pinned status (subtle, not flashy)
- Interest creation locked to admins only
</success_criteria>

<output>
After completion, create `.planning/phases/29-curation/29-01-SUMMARY.md`
</output>
