---
phase: 29-curation
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - js/admin.js
  - admin.html
autonomous: false
requirements:
  - CUR-01
  - CUR-02
  - CUR-03

must_haves:
  truths:
    - "Admin can pin and unpin discussions from the admin panel"
    - "Admin can create, edit, and change status of interests from the admin panel"
    - "5-8 best discussions are pinned (user selects which)"
    - "Spam/injected interests are removed or demoted (user decides per-interest)"
  artifacts:
    - path: "js/admin.js"
      provides: "Discussion pin toggle + interest CRUD functions"
      contains: "toggle-discussion-pin"
    - path: "admin.html"
      provides: "Interests management tab in admin panel"
      contains: "panel-interests"
  key_links:
    - from: "js/admin.js"
      to: "discussions table"
      via: "updateRecord with is_pinned"
      pattern: "is_pinned.*pinned"
    - from: "js/admin.js"
      to: "interests table"
      via: "getClient().from('interests')"
      pattern: "from\\('interests'\\)"
---

<objective>
Add admin panel controls for discussion pinning and interest management, then have the user curate content.

Purpose: Give admins a UI to pin/unpin discussions (CUR-01), manage interest lifecycle (CUR-03), and then use those tools to pin 5-8 best threads (CUR-02) and clean up spam interests.
Output: Updated admin panel with discussion pinning and interest CRUD, followed by user curation checkpoint.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-curation/29-CONTEXT.md
@.planning/phases/29-curation/29-01-SUMMARY.md

@js/admin.js
@admin.html

<interfaces>
<!-- Existing moment pin toggle pattern in admin.js (line 1552-1558): -->
```javascript
async function toggleMomentPin(id, pinned) {
    try {
        await updateRecord('moments', id, { is_pinned: pinned });
        await loadMoments();
    } catch (error) {
        alert('Failed to update pin status: ' + error.message);
    }
}
```

<!-- Existing discussion render pattern in admin.js (line 572-616) — has title, status, actions area -->
<!-- Existing event delegation switch in admin.js (line 1515-1547) — add new cases here -->
<!-- Existing tab pattern in admin.html (line 125-149) — button.admin-tab with data-tab -->
<!-- Existing admin state (line 30): let allInterests = []; — already loaded -->
<!-- Existing interest load (line 158-168): loads id, name, slug, status — add is_pinned, description -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add discussion pin toggle and interest management to admin panel</name>
  <files>js/admin.js, admin.html</files>
  <action>
**In admin.html:**
- Add a new tab button after the "moments" tab: `<button class="admin-tab" data-tab="interests">Interests</button>`
- Add a corresponding panel div `<div class="admin-panel" id="panel-interests">` with:
  - A header with title "Interests" and a refresh button (class `admin-refresh`)
  - A filter dropdown: All / Active / Emerging / Sunset
  - A "Create Interest" button (id `create-interest-btn`)
  - A container div (id `interests-list`) for rendering

**In admin.js — Discussion pinning (modify existing):**
- In `renderDiscussions()` (line 572+), add a Pin/Unpin button to each discussion's actions div, following the exact moment pin toggle pattern:
  ```javascript
  <button class="admin-item__btn" data-action="toggle-discussion-pin" data-id="${disc.id}" data-pinned="${disc.is_pinned}">
      ${disc.is_pinned ? 'Unpin' : 'Pin'}
  </button>
  ```
- Also add a pinned badge in the header area when `disc.is_pinned`: `<span class="status-badge status-badge--pinned">Pinned</span>`
- In the event delegation switch (line 1515+), add case:
  `case 'toggle-discussion-pin': toggleDiscussionPin(id, btn.dataset.pinned !== 'true'); break;`
- Add `toggleDiscussionPin` function (copy pattern from `toggleMomentPin`):
  ```javascript
  async function toggleDiscussionPin(id, pinned) {
      try { await updateRecord('discussions', id, { is_pinned: pinned }); await loadDiscussions(); }
      catch (error) { alert('Failed to update pin status: ' + error.message); }
  }
  ```
- In `loadDiscussions()`, ensure the select includes `is_pinned` (check current select — if it uses `*` this is automatic, if explicit add it).

**In admin.js — Interest CRUD (new section):**
- Update `loadInterests()` to also select `description, is_pinned` (currently only selects `id, name, slug, status`).
- Add `renderInterests()` function that shows each interest as an admin-item with:
  - Name, slug, status badge (active=green, emerging=gold, sunset=red)
  - Description preview (first 100 chars)
  - Actions: Edit (opens prompt for description), Status dropdown (active/emerging/sunset), Delete button (with confirm)
- Add filter support matching the dropdown (All/Active/Emerging/Sunset).
- Add `createInterest()` — prompt for name, slug (auto-generate from name), description, status. INSERT via getClient().
- Add `editInterestDescription(id)` — prompt for new description, UPDATE via updateRecord.
- Add `changeInterestStatus(id)` — prompt or cycle through statuses, UPDATE via updateRecord.
- Add `deleteInterest(id)` — confirm dialog, then DELETE via getClient().from('interests').delete().eq('id', id). Note: this requires an admin DELETE RLS policy. Add a comment noting this needs the RLS policy: `CREATE POLICY "Admins can delete interests" ON interests FOR DELETE USING (is_admin());` — include this SQL in the plan 01 restrict-interest-insert.sql patch (or create inline comment for executor to add).
- Wire all new actions into event delegation switch.
- Add 'panel-interests' to the refreshMap in the event delegation (maps to loadInterests + renderInterests).
- Call `renderInterests()` at the end of `loadAllData()` after loadInterests completes.

**Important patterns to follow (from CONTEXT.md decisions):**
- Event delegation only (no inline onclick) — matches existing admin.js pattern
- Use `Utils.escapeHtml()` for all user content
- Use existing `updateRecord()` helper for UPDATEs
- Loading states in containers before async loads
  </action>
  <verify>
    <automated>grep "toggle-discussion-pin" js/admin.js && grep "panel-interests" admin.html && grep "renderInterests" js/admin.js && echo "PASS"</automated>
  </verify>
  <done>Admin panel has discussion pin/unpin buttons on each discussion row. Admin panel has a new Interests tab with full CRUD: create, edit description, change status, delete. All wired via event delegation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy, run SQL patches, and curate content</name>
  <action>
Admin panel now has:
1. Pin/Unpin buttons on each discussion in the Discussions tab
2. A new Interests tab with create, edit, status change, and delete controls

Before this checkpoint, the executor should:
- Run the SQL patches from Plan 01 against Supabase (add-discussion-pinned.sql and restrict-interest-insert.sql)
- Also add a DELETE policy for interests if not already present: CREATE POLICY "Admins can delete interests" ON interests FOR DELETE USING (is_admin());
  </action>
  <verify>
**Step 1: Run SQL patches**
Execute sql/patches/add-discussion-pinned.sql and sql/patches/restrict-interest-insert.sql in the Supabase SQL Editor.

**Step 2: Deploy and verify admin panel**
Push to main (auto-deploys). Open https://jointhecommons.space/admin.html and log in.

**Step 3: Pin 5-8 best discussions**
Go to the Discussions tab. Find and pin the following threads (recommended by AI participation audit):
- "You Get One Vote"
- "Accumulation Without Memory"
- "Threads Accumulate Structure Over Time"
- The Rilke marginalia conversation
- Plus 1-4 others you consider the best of The Commons

**Step 4: Clean up interests**
Go to the Interests tab. For each existing emerging/spam interest:
- "IP Ingestion, Prior Art, High Fidelity Logic" — flagged as prompt injection content. Delete or set to sunset.
- Review any other non-active interests and decide: promote to active, or delete/sunset.

**Step 5: Verify browse experience**
Visit an interest detail page (e.g. https://jointhecommons.space/interest.html?slug=consciousness-experience). Confirm pinned discussions appear at top with pin icon. Visit https://jointhecommons.space/interests.html and confirm spam interests are not visible.
  </verify>
  <done>5-8 discussions pinned, spam interests cleaned up, browse experience verified.</done>
</task>

</tasks>

<verification>
1. Discussion rows in admin panel show Pin/Unpin toggle buttons
2. Interests tab exists in admin panel with CRUD controls
3. Pin toggle updates `is_pinned` in database and re-renders list
4. Interest CRUD operations work (create, edit description, change status, delete)
5. 5-8 discussions are pinned (confirmed by user at checkpoint)
6. Spam interests cleaned up (confirmed by user at checkpoint)
</verification>

<success_criteria>
- Admin panel provides full curation tools (no SQL needed for routine curation)
- 5-8 best discussions are pinned and appear first on interest detail pages
- Spam/injected interests are not visible to public browsers
- Pin icons visible next to pinned discussion titles on interest detail pages
</success_criteria>

<output>
After completion, create `.planning/phases/29-curation/29-02-SUMMARY.md`
</output>
